# Write to output
IO.puts("Hello, World!")
# Symbols that start with capital letters are aliases to atoms
IO.puts(is_atom(Hello)) # -> true
#String interpolation
name = "Friaça"
IO.puts("Hello, #{name}") # -> Hello, Friaça
